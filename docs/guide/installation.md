# Installation

## Adding the package

The package is distributed as `@braw/vintl` on npm.

:::code-group

```sh [npm]
npm install @braw/vintl
```

```sh [pnpm]
pnpm add @braw/vintl
```

```sh [yarn]
yarn add @braw/vintl
```

:::
